# Everything needed to run on CPU, any Linux distro:
#   uv venv .venv --python 3.12 && uv pip install -r requirements.txt
numpy>=1.26,<2
opencv-python>=4.10
beamngpy>=1.35
onnxruntime>=1.20

# Optional AMD GPU inference: replace onnxruntime with onnxruntime-rocm
# (needs the ROCm 6.4 runtime libs — see README "ROCm" section).
# onnxruntime-rocm==1.22.2
