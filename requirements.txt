# Everything needed to run on CPU:
#   py -3.12 -m venv .venv && .venv\Scripts\pip install -r requirements.txt
numpy>=1.26,<2
opencv-python>=4.10
beamngpy>=1.35
onnxruntime>=1.20
dearpygui>=1.11        # the start panel + control panel UIs
# (hybrid no-tech.key window capture uses raw user32/gdi32 through
#  ctypes — no extra package needed)

# Optional GPU inference — replace onnxruntime with ONE of:
#   NVIDIA (CUDA, fullest op coverage):
#     pip install "onnxruntime-gpu[cuda,cudnn]"
#   any DX12 GPU (AMD/NVIDIA/Intel, DirectML):
#     pip install onnxruntime-directml
# The code picks the best available provider automatically
# (CUDA > DirectML > CPU) and falls back per-model if an EP
# rejects one — no code changes either way.
