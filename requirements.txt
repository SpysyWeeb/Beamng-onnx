# Everything needed to run on CPU:
#   py -3.12 -m venv .venv && .venv\Scripts\pip install -r requirements.txt
numpy>=1.26,<2
opencv-python>=4.10
beamngpy>=1.35
onnxruntime>=1.20
dearpygui>=1.11        # the start panel + control panel UIs
# (hybrid no-tech.key window capture uses raw user32/gdi32 through
#  ctypes — no extra package needed)

# Optional GPU inference (any DX12 GPU — AMD/NVIDIA/Intel):
# replace onnxruntime with onnxruntime-directml. The code already
# prefers DmlExecutionProvider and falls back to CPU automatically.
# onnxruntime-directml
